const book = {
    title: 'Harry Potter',
    pages: 200,
}

let resultKeys = Object.keys(book);
console.log(resultKeys);

let resultValues = Object.values(book);
console.log(resultValues);

let resultEntries = Object.entries(book);
console.log(resultEntries);

